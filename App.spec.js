/* eslint-disable no-undef */
describe('LoadXpert APP Test Suite', () => {

  it('Boolean Equality Check', () => {
    expect(true).toEqual(true);
  });

  it.skip('Should have the correct class', function () {
    expect(hasClass(''), 'does have class ""').to.be.true;
  });

  it.skip('Should have the AppView container', function () {
    expect(find()).to.have.lengthOf(1);
  });


});

